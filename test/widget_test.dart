import 'package:flutter_test/flutter_test.dart';

import 'package:farmer_buyer_marketplace/main.dart';

void main() {
  testWidgets('FarmTrustApp smoke test', (WidgetTester tester) async {
    expect(const FarmTrustApp(), isNotNull);
  });
}

